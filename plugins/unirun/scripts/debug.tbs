import tebasproject;
import tebasplugin;
import tebas;
import "utils.runnables";

if args > 0 & tebasplugin::getResource("runnablesPriority") == "true"{
	if !tryExecSilent(args.0, "debuggables.conf"){
		if !runScript("debug", args.0){
			error("No template script available");
		}
	}
}else{
	if !runScript("debug", args.0){
		if args > 0{
			tebasplugin::runGlobal("debug", args.0);
		}else{
			error("No template script available");
		}
	}
}
