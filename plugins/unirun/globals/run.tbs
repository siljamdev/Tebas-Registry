import stdlib;
import tebas;
import tebasplugin;
import "utils.runnables";

if args.length != 1{
	error("Expected 1 argument: 'file'");
	exit;
}

if !tryExec(args.0, "runnables.conf"){
	error("Impossible to run file");
}
