import stdlib;
import tebas;
import tebasproject;
import tebastemplate;

if args.length != 0{
	error("Expected 0 arguments");
	exit;
}

if !tebasproject::fileExists("globals/tconfig.tbs"){
	tebasproject::fileWrite("globals/tconfig.tbs", tebastemplate::getResource("tconfig"));
}
