import stdlib;
import tebas;
import tebasproject;
import tebastemplate;
import stdnum;
import "utils.ip";

tab ip = [];

if args.length != 1{
	error("Expected 1 argument: 'ip'");
	exit;
}

if isIp(args.0){
	tebasproject::setResource("ip", args.0);
}
