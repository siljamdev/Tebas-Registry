import stdlib;
import tebas;
import tebasproject;
import tebastemplate;
import paths;
import "scripts.build";

if build(){
	tab isWin = getOS() == "windows";
	tebasproject::runProcessDetached(tebasproject::getPath() + "/bin/" + tebasproject::getName() + (isWin ? ".exe" : ""), "bin/", []);
}
