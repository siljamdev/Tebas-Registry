import stdlib;
import tebas;
import tebastemplate;

if args.length != 1{
	error("Expected 1 argument: 'file'");
	exit;
}

tebastemplate::runProcess(tebastemplate::getResource("browserPath"), "", args.0);
