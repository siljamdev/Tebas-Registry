import stdlib;
import tebas;
import tebasproject;
import tebastemplate;

if args.length != 0{
	error("Expected 0 arguments");
	exit;
}

if !tebasproject::fileExists("globals/config.tbs"){
	tebasproject::fileWrite("globals/config.tbs", tebastemplate::getResource("config"));
}
