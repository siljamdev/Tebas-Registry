import stdlib;
import paths;
import tebas;
import tebasproject;
import tebastemplate;

if args.length != 1{
	error("Expected 1 argument: 'file'");
	exit;
}

tebasproject::runProcess(tebastemplate::getResource("fragroomPath"), getDirectory(args.0), ["web", args.0]);
